# 05 — Folder Structure

## Complete Monorepo Layout

```
nexus/
├── apps/
│   ├── api/              ← Fastify backend
│   ├── dashboard/        ← Next.js frontend
│   └── worker/           ← BullMQ background jobs
│
├── packages/
│   ├── database/         ← Prisma schema + client
│   ├── encryption/       ← AES-256-GCM encrypt/decrypt
│   ├── connector-sdk/    ← Framework for building connectors
│   ├── tool-registry/    ← Tool discovery and metadata
│   ├── execution-engine/ ← Core execution logic
│   ├── mcp-runtime/      ← MCP protocol server
│   ├── auth-sdk/         ← JWT + API key utilities
│   ├── logger/           ← Pino structured logging
│   └── shared/           ← Types, constants, schemas
│
├── connectors/
│   ├── gmail/
│   ├── github/
│   ├── slack/
│   └── notion/
│
├── infrastructure/
│   ├── docker/
│   └── scripts/
│
├── .env.example
├── .env                  ← NEVER commit this
├── package.json
├── pnpm-workspace.yaml
├── turbo.json
└── tsconfig.base.json
```

---

## apps/api — Fastify Backend

```
apps/api/
├── src/
│   ├── index.ts              ← Entry point, starts server
│   ├── app.ts                ← Fastify app setup, plugins
│   │
│   ├── routes/
│   │   ├── auth.ts           ← /v1/auth/*
│   │   ├── connections.ts    ← /v1/connections/*
│   │   ├── tools.ts          ← /v1/tools/*
│   │   ├── executions.ts     ← /v1/executions/*
│   │   ├── api-keys.ts       ← /v1/api-keys/*
│   │   ├── workspaces.ts     ← /v1/workspaces/*
│   │   ├── oauth.ts          ← /v1/oauth/* (callback handler)
│   │   └── mcp.ts            ← /v1/mcp/* (MCP endpoints)
│   │
│   ├── middlewares/
│   │   ├── authenticate.ts   ← JWT + API key validation
│   │   ├── rate-limit.ts     ← Per-workspace rate limiting
│   │   ├── validate.ts       ← Zod schema validation
│   │   └── workspace.ts      ← Workspace context injection
│   │
│   ├── services/
│   │   ├── auth.service.ts
│   │   ├── oauth.service.ts
│   │   ├── connection.service.ts
│   │   └── execution.service.ts
│   │
│   ├── errors/
│   │   ├── codes.ts          ← All error codes as constants
│   │   └── handler.ts        ← Global error handler
│   │
│   └── types/
│       └── index.ts          ← Fastify type augmentations
│
├── package.json
└── tsconfig.json
```

---

## apps/dashboard — Next.js Frontend

```
apps/dashboard/
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx                    ← Landing / redirect
│   │   ├── (auth)/
│   │   │   ├── login/page.tsx
│   │   │   └── signup/page.tsx
│   │   └── (dashboard)/
│   │       ├── layout.tsx              ← Sidebar + nav
│   │       ├── page.tsx                ← Overview
│   │       ├── connections/
│   │       │   └── page.tsx            ← Connect apps
│   │       ├── tools/
│   │       │   └── page.tsx            ← Browse tools
│   │       ├── executions/
│   │       │   ├── page.tsx            ← Execution list
│   │       │   └── [id]/page.tsx       ← Execution detail
│   │       ├── api-keys/
│   │       │   └── page.tsx
│   │       └── settings/
│   │           └── page.tsx
│   │
│   ├── components/
│   │   ├── ui/                         ← shadcn components
│   │   ├── layout/
│   │   │   ├── sidebar.tsx
│   │   │   └── header.tsx
│   │   ├── connections/
│   │   │   ├── connector-card.tsx
│   │   │   └── connect-button.tsx
│   │   ├── executions/
│   │   │   ├── execution-table.tsx
│   │   │   └── execution-detail.tsx
│   │   └── shared/
│   │       ├── status-badge.tsx
│   │       └── copy-button.tsx
│   │
│   ├── hooks/
│   │   ├── use-connections.ts
│   │   ├── use-executions.ts
│   │   └── use-workspace.ts
│   │
│   ├── lib/
│   │   ├── api.ts            ← API client (fetch wrapper)
│   │   └── utils.ts
│   │
│   └── types/
│       └── index.ts
│
├── package.json
└── tsconfig.json
```

---

## apps/worker — Background Jobs

```
apps/worker/
├── src/
│   ├── index.ts              ← Starts all workers
│   ├── queues/
│   │   ├── execution.queue.ts    ← Async tool execution queue
│   │   └── token-refresh.queue.ts
│   └── workers/
│       ├── execution.worker.ts   ← Processes execution jobs
│       └── token-refresh.worker.ts
│
├── package.json
└── tsconfig.json
```

---

## packages/database

```
packages/database/
├── prisma/
│   ├── schema.prisma         ← Full schema (see 04-database.md)
│   ├── seed.ts
│   └── migrations/           ← Auto-generated by Prisma
├── src/
│   └── index.ts              ← Export Prisma client singleton
├── package.json
└── tsconfig.json
```

```typescript
// packages/database/src/index.ts
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as {
  prisma: PrismaClient | undefined
}

export const prisma =
  globalForPrisma.prisma ??
  new PrismaClient({
    log: process.env.NODE_ENV === 'development' 
      ? ['query', 'error', 'warn'] 
      : ['error'],
  })

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma
}

export * from '@prisma/client'
```

---

## packages/encryption

```
packages/encryption/
├── src/
│   └── index.ts    ← encrypt() and decrypt() functions
├── package.json
└── tsconfig.json
```

---

## packages/connector-sdk

```
packages/connector-sdk/
├── src/
│   ├── index.ts
│   ├── define-connector.ts   ← defineConnector() function
│   ├── define-action.ts      ← defineAction() function
│   ├── types.ts              ← ConnectorDef, ActionDef, etc
│   └── validate.ts           ← Schema validation
├── package.json
└── tsconfig.json
```

---

## packages/execution-engine

```
packages/execution-engine/
├── src/
│   ├── index.ts
│   ├── engine.ts             ← Main ExecutionEngine class
│   ├── steps/
│   │   ├── validate-tool.ts
│   │   ├── check-permissions.ts
│   │   ├── get-token.ts
│   │   ├── invoke-connector.ts
│   │   └── log-result.ts
│   ├── retry.ts              ← Retry logic with backoff
│   ├── idempotency.ts        ← Idempotency key checking
│   └── types.ts
├── package.json
└── tsconfig.json
```

---

## packages/mcp-runtime

```
packages/mcp-runtime/
├── src/
│   ├── index.ts
│   ├── server.ts             ← MCP server setup
│   ├── handlers/
│   │   ├── tools-list.ts     ← Handle tools/list requests
│   │   └── tools-call.ts     ← Handle tools/call requests
│   ├── sessions.ts           ← Redis session management
│   └── types.ts
├── package.json
└── tsconfig.json
```

---

## connectors/gmail

```
connectors/gmail/
├── src/
│   ├── index.ts              ← Export connector definition
│   ├── manifest.ts           ← Connector metadata
│   ├── auth.ts               ← OAuth config (scopes, URLs)
│   ├── actions/
│   │   ├── send-email.ts
│   │   ├── list-emails.ts
│   │   ├── get-email.ts
│   │   ├── search-emails.ts
│   │   ├── reply-email.ts
│   │   └── create-draft.ts
│   ├── schemas/
│   │   ├── send-email.schema.ts
│   │   └── list-emails.schema.ts
│   └── utils/
│       └── format-message.ts     ← RFC 2822 email formatting
├── package.json
└── tsconfig.json
```

---

## Root Config Files

```json
// pnpm-workspace.yaml
packages:
  - 'apps/*'
  - 'packages/*'
  - 'connectors/*'
```

```json
// turbo.json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "lint": {}
  }
}
```

```json
// tsconfig.base.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

---

## Package Dependencies Map

```
apps/api depends on:
  packages/database
  packages/encryption
  packages/connector-sdk
  packages/tool-registry
  packages/execution-engine
  packages/mcp-runtime
  packages/auth-sdk
  packages/logger
  packages/shared

apps/dashboard depends on:
  packages/shared (types only)

apps/worker depends on:
  packages/database
  packages/execution-engine
  packages/logger

connectors/* depend on:
  packages/connector-sdk
  packages/shared

FORBIDDEN:
  connectors/* → packages/database (direct)
  connectors/* → packages/execution-engine (direct)
  connectors/* → other connectors
```
